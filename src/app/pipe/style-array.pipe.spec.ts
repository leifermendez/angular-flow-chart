import { StyleArrayPipe } from './style-array.pipe';

describe('StyleArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleArrayPipe();
    expect(pipe).toBeTruthy();
  });
});

import { StyleArrayDirective } from './style-array.directive';

describe('StyleArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleArrayDirective();
    expect(directive).toBeTruthy();
  });
});
